import React from 'react';

// we either will have 2 argument or 4 depending on what we decide.
const LandingPage = ({ token, posts, user, }) => {
  const navigate = useNavigate();
  return 
    
  
}




<div className='user-profile'>
<button onClick={() => { navigate('/Login/Register') }}>You need to 
Login/Register to use this page</button>;
</div>


export default LandingPage;
